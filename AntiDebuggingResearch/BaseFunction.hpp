#pragma once

#include <vector>

void BaseFunction5(std::vector<size_t>& iDataVector, size_t i);

void BaseFunction5([[maybe_unused]] std::vector<size_t>& iDataVector, [[maybe_unused]] size_t i);

void BaseFunction5([[maybe_unused]] std::vector<size_t>& iDataVector, [[maybe_unused]] size_t i);

